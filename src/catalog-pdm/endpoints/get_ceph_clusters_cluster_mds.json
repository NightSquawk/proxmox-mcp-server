{
  "operationId": "get_ceph_clusters_cluster_mds",
  "surface": "pdm",
  "resource": "ceph",
  "category": "Ceph",
  "name": "mds",
  "method": "GET",
  "path": "/ceph/clusters/{cluster}/mds",
  "description": "List the cluster's Ceph metadata servers (MDS).",
  "writeOperation": false,
  "destructive": false,
  "protected": false,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "cluster",
      "type": "string",
      "required": true,
      "description": "Ceph cluster identifier (fsid).",
      "minLength": 8,
      "maxLength": 64,
      "pattern": "/^(?:[A-Za-z0-9_][A-Za-z0-9._\\-]*)$/"
    }
  ],
  "params": [],
  "returns": {
    "description": "Ceph metadata servers of the cluster.",
    "items": {
      "additionalProperties": false,
      "description": "Object.",
      "properties": {
        "addr": {
          "description": "Address as advertised by the MDS; Ceph-formatted (typically\n'IP:PORT/NONCE').",
          "optional": 1,
          "type": "string"
        },
        "ceph_version": {
          "description": "Full Ceph version string of the MDS daemon.",
          "optional": 1,
          "type": "string"
        },
        "ceph_version_short": {
          "description": "Short Ceph version string of the MDS daemon (e.g. '19.2.0').",
          "optional": 1,
          "type": "string"
        },
        "direxists": {
          "default": false,
          "description": "Set when the MDS's data directory exists on this node.",
          "optional": 1,
          "type": "boolean"
        },
        "fs_name": {
          "description": "Name of the CephFS this MDS is bound to; absent or null for standby\nMDSes not currently serving a rank.",
          "optional": 1,
          "type": "string"
        },
        "host": {
          "description": "Host the MDS runs on.",
          "optional": 1,
          "type": "string"
        },
        "name": {
          "description": "The name (ID) for the MDS.",
          "type": "string"
        },
        "rank": {
          "description": "MDS rank within the file system; -1 for standby MDSes not currently\nbound to a rank.",
          "optional": 1,
          "type": "integer"
        },
        "service": {
          "default": false,
          "description": "Set if a ceph-mds@<id> systemd unit is enabled on the hosting node;\nabsent otherwise.",
          "optional": 1,
          "type": "boolean"
        },
        "standby_replay": {
          "default": false,
          "description": "If true, the standby MDS is polling the active MDS for faster recovery\n(hot standby).",
          "optional": 1,
          "type": "boolean"
        },
        "state": {
          "description": "MDS state: Ceph-reported run state (e.g. 'up:active', 'up:standby',\n'up:standby-replay') for daemons known to the cluster; 'stopped' or\n'unknown' for configured daemons not visible to the cluster.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "type": "array"
  },
  "permissions": {
    "user": "all"
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PDMAPIToken=USER@REALM!TOKENID=SECRET' 'https://PDM_HOST:8443/api2/json/ceph/clusters/<cluster>/mds'"
}
