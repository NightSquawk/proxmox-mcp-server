{
  "operationId": "get_pve_remotes_remote_tasks_upid_status",
  "surface": "pdm",
  "resource": "pve",
  "category": "PVE Remotes",
  "name": "status",
  "method": "GET",
  "path": "/pve/remotes/{remote}/tasks/{upid}/status",
  "description": "Get the status of a task from a Proxmox VE instance.",
  "writeOperation": false,
  "destructive": false,
  "protected": false,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "remote",
      "type": "string",
      "required": true,
      "description": "Remote ID.",
      "minLength": 2,
      "maxLength": 32,
      "pattern": "/^(?:[A-Za-z0-9_][A-Za-z0-9._\\-]*)$/"
    },
    {
      "name": "upid",
      "type": "string",
      "required": true,
      "description": "A remote UPID",
      "minLength": 42
    }
  ],
  "params": [
    {
      "name": "wait",
      "type": "boolean",
      "required": false,
      "description": "wait for the task to finish before returning its result",
      "default": false
    }
  ],
  "returns": {
    "additionalProperties": false,
    "description": "Object.",
    "properties": {
      "exitstatus": {
        "description": "The task's exit status.",
        "optional": 1,
        "type": "string"
      },
      "id": {
        "description": "The task id.",
        "type": "string"
      },
      "node": {
        "description": "The task's node.",
        "type": "string"
      },
      "pid": {
        "description": "The task process id.",
        "type": "integer"
      },
      "pstart": {
        "description": "The task's proc start time.",
        "type": "integer"
      },
      "starttime": {
        "description": "The task's start time.",
        "type": "integer"
      },
      "status": {
        "description": "A guest's run state.",
        "enum": [
          "running",
          "stopped"
        ],
        "type": "string"
      },
      "type": {
        "description": "The task type.",
        "type": "string"
      },
      "upid": {
        "description": "The task's UPID.",
        "type": "string"
      },
      "user": {
        "description": "The task owner's user id.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "permissions": {
    "check": {
      "partial": false,
      "path": [
        "resource",
        "{remote}"
      ],
      "privs": [
        "Resource.Audit"
      ]
    }
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PDMAPIToken=USER@REALM!TOKENID=SECRET' 'https://PDM_HOST:8443/api2/json/pve/remotes/<remote>/tasks/<upid>/status'"
}
