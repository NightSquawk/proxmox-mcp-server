{
  "operationId": "get_pve_remotes_remote_cluster_nextid",
  "surface": "pdm",
  "resource": "pve",
  "category": "PVE Remotes",
  "name": "cluster-nextid",
  "method": "GET",
  "path": "/pve/remotes/{remote}/cluster-nextid",
  "description": "Get the next free VMID on the (target) cluster, e.g. to prefill a migration target VMID.",
  "writeOperation": false,
  "destructive": false,
  "protected": false,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "remote",
      "type": "string",
      "required": true,
      "description": "Remote ID.",
      "minLength": 2,
      "maxLength": 32,
      "pattern": "/^(?:[A-Za-z0-9_][A-Za-z0-9._\\-]*)$/"
    }
  ],
  "params": [
    {
      "name": "target-endpoint",
      "type": "string",
      "required": false,
      "description": "The target endpoint to use for the connection."
    }
  ],
  "returns": {
    "type": "null"
  },
  "permissions": {
    "check": {
      "partial": false,
      "path": [
        "resource",
        "{remote}"
      ],
      "privs": [
        "Resource.Audit"
      ]
    }
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PDMAPIToken=USER@REALM!TOKENID=SECRET' 'https://PDM_HOST:8443/api2/json/pve/remotes/<remote>/cluster-nextid'"
}
