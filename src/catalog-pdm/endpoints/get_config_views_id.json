{
  "operationId": "get_config_views_id",
  "surface": "pdm",
  "resource": "config",
  "category": "Configuration",
  "name": "{id}",
  "method": "GET",
  "path": "/config/views/{id}",
  "description": "Get the config of a single view.",
  "writeOperation": false,
  "destructive": false,
  "protected": false,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "View name.",
      "minLength": 2,
      "maxLength": 32,
      "pattern": "/^(?:[A-Za-z0-9_][A-Za-z0-9._\\-]*)$/"
    }
  ],
  "params": [],
  "returns": {
    "additionalProperties": false,
    "description": "View definition",
    "properties": {
      "exclude": {
        "description": "List of filter rules.",
        "items": {
          "description": "Filter rule for resources.",
          "type": "string",
          "typetext": "[exact:]resource-type=<storage|qemu|lxc|sdn-zone|datastore|node>|[exact:]resource-pool=<pool-name>|[exact:]tag=<tag-name>|[exact:]remote=<remote-name>|[exact:]resource=id:<resource-id>"
        },
        "optional": 1,
        "type": "array"
      },
      "id": {
        "description": "View name.",
        "maxLength": 32,
        "minLength": 2,
        "pattern": "/^(?:[A-Za-z0-9_][A-Za-z0-9._\\-]*)$/",
        "type": "string"
      },
      "include": {
        "description": "List of filter rules.",
        "items": {
          "description": "Filter rule for resources.",
          "type": "string",
          "typetext": "[exact:]resource-type=<storage|qemu|lxc|sdn-zone|datastore|node>|[exact:]resource-pool=<pool-name>|[exact:]tag=<tag-name>|[exact:]remote=<remote-name>|[exact:]resource=id:<resource-id>"
        },
        "optional": 1,
        "type": "array"
      },
      "include-all": {
        "description": "Include all resources by default.",
        "optional": 1,
        "type": "boolean"
      },
      "layout": {
        "description": "The configured layout, encoded as json",
        "optional": 1,
        "type": "string"
      }
    },
    "type": "object"
  },
  "permissions": {
    "check": {
      "partial": false,
      "path": [
        "view",
        "{id}"
      ],
      "privs": [
        "Resource.Audit"
      ]
    }
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PDMAPIToken=USER@REALM!TOKENID=SECRET' 'https://PDM_HOST:8443/api2/json/config/views/<id>'"
}
