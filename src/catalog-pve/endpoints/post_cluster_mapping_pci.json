{
  "operationId": "post_cluster_mapping_pci",
  "surface": "pve",
  "resource": "cluster",
  "category": "Cluster",
  "name": "create",
  "method": "POST",
  "path": "/cluster/mapping/pci",
  "description": "Create a new hardware mapping.",
  "writeOperation": true,
  "destructive": false,
  "protected": true,
  "allowtoken": true,
  "paramLocation": "body",
  "pathParams": [],
  "params": [
    {
      "name": "description",
      "type": "string",
      "required": false,
      "description": "Description of the logical PCI device.",
      "maxLength": 4096,
      "typetext": "<string>"
    },
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The ID of the logical PCI mapping.",
      "format": "pve-configid",
      "typetext": "<string>"
    },
    {
      "name": "live-migration-capable",
      "type": "boolean",
      "required": false,
      "description": "Marks the device(s) as being able to be live-migrated (Experimental). This needs hardware and driver support to work.",
      "default": 0,
      "typetext": "<boolean>"
    },
    {
      "name": "map",
      "type": "array",
      "required": true,
      "description": "A list of maps for the cluster nodes.",
      "typetext": "<array>"
    },
    {
      "name": "mdev",
      "type": "boolean",
      "required": false,
      "description": "Marks the device(s) as being capable of providing mediated devices.",
      "default": 0,
      "typetext": "<boolean>"
    }
  ],
  "returns": {
    "type": "null"
  },
  "permissions": {
    "check": [
      "perm",
      "/mapping/pci",
      [
        "Mapping.Modify"
      ]
    ]
  },
  "requestSample": "curl -k -X POST -H 'Authorization: PVEAPIToken=USER@REALM!TOKENID=SECRET' -H 'Content-Type: application/json' \\\n  -d '{\"id\":\"<id>\",\"map\":\"<map>\"}' 'https://PVE_HOST:8006/api2/json/cluster/mapping/pci'"
}
