{
  "operationId": "get_nodes_node_rrd",
  "surface": "pve",
  "resource": "nodes",
  "category": "Nodes & Guests",
  "name": "rrd",
  "method": "GET",
  "path": "/nodes/{node}/rrd",
  "description": "Read node RRD statistics (returns PNG)",
  "writeOperation": false,
  "destructive": false,
  "protected": true,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "node",
      "type": "string",
      "required": true,
      "description": "The cluster node name.",
      "format": "pve-node",
      "typetext": "<string>"
    }
  ],
  "params": [
    {
      "name": "cf",
      "type": "string",
      "required": false,
      "description": "The RRD consolidation function",
      "enum": [
        "AVERAGE",
        "MAX"
      ]
    },
    {
      "name": "ds",
      "type": "string",
      "required": true,
      "description": "The list of datasources you want to display.",
      "format": "pve-configid-list",
      "typetext": "<string>"
    },
    {
      "name": "timeframe",
      "type": "string",
      "required": true,
      "description": "Specify the time frame you are interested in.",
      "enum": [
        "hour",
        "day",
        "week",
        "month",
        "year",
        "decade"
      ]
    }
  ],
  "returns": {
    "properties": {
      "filename": {
        "type": "string"
      }
    },
    "type": "object"
  },
  "permissions": {
    "check": [
      "perm",
      "/nodes/{node}",
      [
        "Sys.Audit"
      ]
    ]
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PVEAPIToken=USER@REALM!TOKENID=SECRET' 'https://PVE_HOST:8006/api2/json/nodes/<node>/rrd?ds=<ds>&timeframe=hour'"
}
