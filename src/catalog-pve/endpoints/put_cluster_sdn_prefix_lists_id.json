{
  "operationId": "put_cluster_sdn_prefix_lists_id",
  "surface": "pve",
  "resource": "cluster",
  "category": "Cluster",
  "name": "update_prefix_list",
  "method": "PUT",
  "path": "/cluster/sdn/prefix-lists/{id}",
  "description": "Update Prefix List",
  "writeOperation": true,
  "destructive": false,
  "protected": true,
  "allowtoken": true,
  "paramLocation": "body",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The SDN prefix list identifier",
      "format": "pve-sdn-prefix-list-id",
      "typetext": "<string>"
    }
  ],
  "params": [
    {
      "name": "delete",
      "type": "array",
      "required": false,
      "typetext": "<array>"
    },
    {
      "name": "digest",
      "type": "string",
      "required": false,
      "description": "Prevent changes if current configuration file has a different digest. This can be used to prevent concurrent modifications.",
      "maxLength": 64,
      "typetext": "<string>"
    },
    {
      "name": "entries",
      "type": "array",
      "required": false,
      "typetext": "<array>"
    },
    {
      "name": "lock-token",
      "type": "string",
      "required": false,
      "description": "the token for unlocking the global SDN configuration",
      "typetext": "<string>"
    }
  ],
  "returns": {
    "type": "null"
  },
  "permissions": {
    "check": [
      "perm",
      "/sdn/prefix-lists/{id}",
      [
        "SDN.Allocate"
      ]
    ]
  },
  "requestSample": "curl -k -X PUT -H 'Authorization: PVEAPIToken=USER@REALM!TOKENID=SECRET' -H 'Content-Type: application/json' \\\n  -d '{}' 'https://PVE_HOST:8006/api2/json/cluster/sdn/prefix-lists/<id>'"
}
