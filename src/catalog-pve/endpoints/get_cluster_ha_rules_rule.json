{
  "operationId": "get_cluster_ha_rules_rule",
  "surface": "pve",
  "resource": "cluster",
  "category": "Cluster",
  "name": "read_rule",
  "method": "GET",
  "path": "/cluster/ha/rules/{rule}",
  "description": "Read HA rule.",
  "writeOperation": false,
  "destructive": false,
  "protected": false,
  "allowtoken": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "rule",
      "type": "string",
      "required": true,
      "description": "HA rule identifier.",
      "format": "pve-configid",
      "typetext": "<string>"
    }
  ],
  "params": [],
  "returns": {
    "properties": {
      "rule": {
        "description": "HA rule identifier.",
        "format": "pve-configid",
        "type": "string"
      },
      "type": {
        "description": "HA rule type.",
        "enum": [
          "node-affinity",
          "resource-affinity"
        ],
        "type": "string"
      }
    },
    "type": "object"
  },
  "permissions": {
    "check": [
      "perm",
      "/",
      [
        "Sys.Audit"
      ]
    ]
  },
  "requestSample": "curl -k -X GET -H 'Authorization: PVEAPIToken=USER@REALM!TOKENID=SECRET' 'https://PVE_HOST:8006/api2/json/cluster/ha/rules/<rule>'"
}
