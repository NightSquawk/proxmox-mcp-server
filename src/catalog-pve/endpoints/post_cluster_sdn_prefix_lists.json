{
  "operationId": "post_cluster_sdn_prefix_lists",
  "surface": "pve",
  "resource": "cluster",
  "category": "Cluster",
  "name": "create_prefix_list_entry",
  "method": "POST",
  "path": "/cluster/sdn/prefix-lists",
  "description": "Create Prefix List",
  "writeOperation": true,
  "destructive": false,
  "protected": true,
  "allowtoken": true,
  "paramLocation": "body",
  "pathParams": [],
  "params": [
    {
      "name": "digest",
      "type": "string",
      "required": false,
      "description": "Prevent changes if current configuration file has a different digest. This can be used to prevent concurrent modifications.",
      "maxLength": 64,
      "typetext": "<string>"
    },
    {
      "name": "entries",
      "type": "array",
      "required": false,
      "typetext": "<array>"
    },
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The SDN prefix list identifier",
      "format": "pve-sdn-prefix-list-id",
      "typetext": "<string>"
    },
    {
      "name": "lock-token",
      "type": "string",
      "required": false,
      "description": "the token for unlocking the global SDN configuration",
      "typetext": "<string>"
    }
  ],
  "returns": {
    "type": "null"
  },
  "permissions": {
    "check": [
      "perm",
      "/sdn/prefix-lists",
      [
        "SDN.Allocate"
      ]
    ]
  },
  "requestSample": "curl -k -X POST -H 'Authorization: PVEAPIToken=USER@REALM!TOKENID=SECRET' -H 'Content-Type: application/json' \\\n  -d '{\"id\":\"<id>\"}' 'https://PVE_HOST:8006/api2/json/cluster/sdn/prefix-lists'"
}
